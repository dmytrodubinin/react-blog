{
  "name": "mern-blog-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server --prefix api\" \"npm run dev --prefix client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
